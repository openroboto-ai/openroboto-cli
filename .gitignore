# Python
**/__pycache__/
*.py[cod]
*.so
.venv/
venv/

# Secrets and local configuration
# 矿工的 miner.yaml 里有 HF token 和钱包密码，所以整类 *.yaml 默认不进版本库。
*.yaml
!*.example.yaml
!**/*.example.yaml
# 例外：打进 pip 包的配置模板。它们**必须**进版本库 —— 被 *.yaml 规则吞掉的话，
# 从干净 clone 构建出来的 wheel 里没有模板，`openroboto init` 当场失败。
!src/openroboto/templates/*.yaml
# 例外：本地钩子配置。它必须进版本库，否则每个人的钩子各不相同 ——
# 实测 `git check-ignore -v .pre-commit-config.yaml` 命中上面的 `*.yaml`，
# 加它进来时会静默失败（`git add` 不报错，文件就是没进去）。
!.pre-commit-config.yaml
.env
.env.*

# Runtime data
*.db
*.sqlite
*.sqlite3
state/
.pids/
logs/
tmp/
*.pid

# Build and tool caches
dist/
build/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
# coverage 的中间数据、逐行报告，以及 CI 那条 --junitxml 的产物
.coverage
htmlcov/
pytest-report.xml
# 基座 checkpoint 的本地缓存（几个 GB）
cache/

# Model artifacts
*.pt
*.pth
*.ckpt

# OS and editor files
.DS_Store
Thumbs.db


# JetBrains IDE
.idea/

# 内部工程规范（中文，面向团队与 agent），**不进公开仓**。
# 2026-08-19 从全部历史里移除后加入忽略清单 —— 这个仓是公开的，
# 而这几份写的是内部推理、未修缺陷清单和跨仓约定，不是给外部矿工看的。
# 本地仍然有这几个文件，它们是项目规范的唯一来源；别把它们加回版本库。
AGENTS.md
CLAUDE.md
SCOPE.md
